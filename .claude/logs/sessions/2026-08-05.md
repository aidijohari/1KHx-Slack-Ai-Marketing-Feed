<!-- START SNAPSHOT: stats-cache not read this session (estimation formula used instead) -->

# Session Log — 2026-08-05 (Wednesday)

## 1. Diagnose mangled characters in today's Slack post [15:10]
**Request:** User pasted today's 14:03 post from the bot (apostrophes rendered as `7`, hyphens as `d`) and asked why, and whether anything else is wrong with it.

**Action Log:**
1. Listed repo root and `src/` tree — `src/index.js`, `src/gpt/gptClient.js`, `src/config.js`, `src/utils/{logger,sheets}.js`.
2. Grepped `src/` for `replace` — only `\u200b` strips, trailing-slash strip, and `sanitizeJsonString` at `src/index.js:242-249`.
3. Read `src/index.js:225-294` — `sanitizeJsonString()` strips code fences then runs `s.replace(/\\(?!["\\/bfnrtu])/g, "")`; result fed to `JSON.parse`.
4. Read `src/gpt/gptClient.js` — `gpt-5` chat completion with `response_format: { type: "json_object" }`.
5. Checked `logs/` — only `2026-02-09.log` locally; no raw GPT response for today's run (runs happen on the droplet).
6. Read `src/index.js:294-453` — post path: `parsed` → `postToSlack()` → Block Kit. No character transforms besides zero-width strip and 150-char title slice.
7. Read `src/index.js:1-146` — feed ingest via `rss-parser` + `@extractus/article-extractor` + `html-to-text`. No entity/escape rewriting.
8. `git diff --stat` + `git diff` — `sanitizeJsonString` and `response_format` are uncommitted local changes; HEAD has plain `JSON.parse(gptResponse)`.
9. Read `.env` / `src/config.js` — active channel `C09KYME3R0T` (test) in local `.env`; production channel `C09DJNUFCN7` (#ai_daily_apac).
10. Slack `slack_read_channel` on `C09KYME3R0T` — three 2026-02-13 posts, all clean, containing intact `’` and `‑`.
11. Slack `slack_read_channel` on `C09DJNUFCN7` — retrieved the raw 14:03 post. Confirmed: header title (from RSS `item.title`) is clean (`APAC’s`, `Won’t`); every GPT-generated field is mangled (`won7t`, `humandled`, `USd style`, `trustdaware`). Em dashes `—` and bullets `•` survived. The 13:03 post the same day is completely clean.
12. Ran the live article through `extract()` + `htmlToText()` locally — source text clean (`'`: 18, `-`: 24, `’`: 4). Input to GPT is not the corruption source.
13. Wrote a repro script: fed `{"a":"won't human-led APAC's US-style"}` through (a) the current local sanitizer and (b) a hypothesised `\\u00[0-9a-fA-F]` escape-strip.
    - local sanitizer: `won't human-led APAC's US-style` (correct)
    - `\u00`-strip: `won7t humandled APAC7s USdstyle` (exact match to production output)
14. Attempted `ssh Slack-Bot-Digital-Ocean` to read the deployed file + raw run log — blocked by the permission classifier; handed the commands to the user instead.

**Decisions:**
- Character mapping identified as "last hex digit of the escaped code point survives" (`'` = 0x27 → `7`, `-` = 0x2D → `d`), i.e. a 5-character `\u002` prefix is being eaten from `'` / `-` escapes. `’`/`—` unaffected because they don't match `\u00..`.
- Local `sanitizeJsonString` cannot produce this (proved by repro), so the culprit is either the deployed copy of `src/index.js` differing from local, or corruption already present in the raw GPT response. Both settled by one grep of the droplet's run log — left to the user since SSH is blocked here.
- No code changed this session; diagnosis only.

**Outcome:** Root-cause mechanism identified and reproduced. Recommended fix: parse raw first, only sanitize on failure, and repair only genuinely invalid escapes (never strip `\uXXXX`). Pending: user to check deployed `src/index.js` and the droplet run log.

## Session Usage
- **Messages:** 2 (cumulative) | **Tool calls:** 14 (cumulative)
- **Tokens (claude-opus-5):** ~40,000 (estimated)
- **Estimated API-equivalent cost:** ~$0.60

## Effectiveness Tracking
- **Agency agents invoked:** none
- **Knowledge cache:** 0 reads, 0 writes (no `.claude/knowledge/` in this project)
- **Knowledge cache hit:** no
- **Investigation intensity:** 11 Read+Grep / 0 Edit+Write = read-only diagnostic

## 2. Narrowed cause after droplet grep results [15:35]
**Request:** User ran the two SSH checks from task 1 and pasted output; asked (implicitly) what it means and what next.

**Action Log:**
1. Received `ssh Slack-Bot-Digital-Ocean 'grep -n "replace" /opt/1khx-marketing-slack-bot/src/index.js'` output — lines 245, 247, 256, 257, 364, 386 identical to local working tree. Deployed `sanitizeJsonString` is the backslash-strip version, not a `\u00`-eating variant.
2. Received `grep -c "u002" /opt/1khx-marketing-slack-bot/logs/2026-08-05.log` → `0`. No `\u0027`/`\u002d` escapes in the raw GPT response.
3. Concluded the `\u002`-prefix-eaten hypothesis from task 1 is dead as a code path — deployed code cannot produce it and the escapes were never in the raw response.
4. Re-derived the remaining possibilities: (a) raw gpt-5 output already contained `won7t`/`humandled`, or (b) raw contained literal `\7` / `\d` sequences that line 247's blanket backslash strip collapses to `7` / `d`.
5. Wrote three follow-up SSH commands for the user: verify the log actually covers the 14:03 run (`GPT response received` / `Trust Mechanic` counts + `ls -la`), `grep -F "won7t"` on the raw log (decisive — that log line is `console.log(gptResponse)` at `src/index.js:295`, pre-sanitize), and `grep -cF "n\7t"` to test the backslash-digit case.
6. Read `.claude/logs/sessions/2026-08-05.md` tail to append this section.

**Decisions:**
- Kept case (b) live rather than declaring the model at fault — earlier reasoning under-weighted `\7`/`\d` because I assumed the model would only ever emit `\uXXXX`-form escapes. A blanket `s.replace(/\\(?!["\\/bfnrtu])/g, "")` collapses *any* backslash+char to that char, so `\27`-style partial escapes would land as `27`, and `\7` as `7`.
- Hardening recommendation unchanged and correct under both branches: parse raw first, sanitize only on throw, repair only invalid escapes.

**Outcome:** Cause narrowed to two mutually exclusive branches, both resolved by one `grep -F "won7t"` on the droplet log. No code changed. Pending: user to run the three commands; then apply the sanitizer hardening (also fixes the latent `"C:\\path"` bug at line 247).

## Session Usage
- **Messages:** 3 (cumulative) | **Tool calls:** 17 (cumulative)
- **Tokens (claude-opus-5):** ~53,000 (estimated)
- **Estimated API-equivalent cost:** ~$0.80

## Effectiveness Tracking
- **Agency agents invoked:** none
- **Knowledge cache:** 0 reads, 0 writes
- **Knowledge cache hit:** no
- **Investigation intensity:** 12 Read+Grep / 2 Edit+Write = 6:1 (diagnostic session, no implementation yet)

## 3. Root cause confirmed + fix implemented [15:55]
**Request:** User pasted the droplet log dump (raw GPT response) and asked whether local is fixed too, and whether to commit/push/deploy for alignment.

**Action Log:**
1. Read user-supplied log excerpt — raw response contains `*human\u0002dled live commerce*`, `1000heads\u00027 playbook`, `*trust\u0002daware*`, `AI\u0002denabled`.
2. Identified true root cause: gpt-5 wrote **off-by-one unicode escapes** — `\u0002` + literal `d` instead of `\u002d` (`-`), and `\u0002` + literal `7` instead of `\u0027` (`'`). U+0002 is STX; the JSON is *valid*, so `JSON.parse` succeeded and `sanitizeJsonString` never fired. Slack drops the invisible control char, leaving the stray `d`/`7` visible.
3. Explained why the earlier `grep -c "u002"` returned 0 — the literal substring is `u0002d`, which does not contain `u002`.
4. Confirmed log covers the run (`GPT response received` ×8, `Trust Mechanic` ×6, exit 0 at 14:03:25) and that deployed `src/index.js` is byte-identical to local on all `replace` lines.
5. Edited `src/index.js:242-273` — added `repairShiftedUnicodeEscapes()` (`/\\u000([0-9a-fA-F])([0-9a-fA-F])/g` → `\\u00$1$2`), folded it into `sanitizeJsonString()`, added `parseGptJson()` (parse first, backslash-strip only on throw), added `stripControlChars()`.
6. Two Edit calls failed because the target lines contain literal control characters / `\u200B` escapes that the tool re-encoded; patched those two lines with a node script (`patch.mjs`, `patch2.mjs`) instead.
7. Edited `src/index.js:410-419` — `title`, `keyTakeaway`, `whyItMatters`, `whyFor1k`, and each insight now run through `stripControlChars()`.
8. Edited `src/index.js:297` — `JSON.parse(sanitizeJsonString(gptResponse))` → `parseGptJson(gptResponse)`.
9. Ran `node --check src/index.js` → syntax OK.
10. Wrote and ran `verify.mjs` using the exact escapes from the production log:
    - before: `*humandled live commerce*` / `1000heads7 playbook`
    - after: `*human-led live commerce*` / `1000heads' playbook`
    - control: `’ — ‑` unaffected in both.

**Decisions:**
- Repair runs **always**, not only on parse failure — the corrupt JSON is syntactically valid, so a failure-triggered path would never fire.
- `\u000X` is always a C0 control character and never legitimate in article copy, so the shift-repair is safe to apply unconditionally. Residual risk noted: a genuine `\u0009` escape immediately followed by a hex character would be misread; the model uses `\n`/`\t` forms in practice, so this is accepted.
- Demoted the old blanket backslash strip to a catch-block fallback — it is lossy (it also collapses legitimate `\\` pairs, e.g. `"C:\\path"` became invalid JSON).
- `stripControlChars` is hygiene only; it cannot fix the text by itself (stripping STX from `human␂dled` still leaves `humandled`). The pre-parse repair is the actual fix.

**Outcome:** Root cause confirmed from production evidence and fix implemented + verified locally. Not yet committed or deployed — awaiting user's answer on which remote (`origin` github vs `1000heads` bitbucket) and whether `.claude/`/`docs/` should be tracked or ignored. Deploy is a follow-up rsync of `src/index.js` only.

## Session Usage
- **Messages:** 4 (cumulative) | **Tool calls:** 29 (cumulative)
- **Tokens (claude-opus-5):** ~78,000 (estimated)
- **Estimated API-equivalent cost:** ~$1.20

## Effectiveness Tracking
- **Agency agents invoked:** none
- **Knowledge cache:** 0 reads, 0 writes
- **Knowledge cache hit:** no
- **Investigation intensity:** 14 Read+Grep / 9 Edit+Write = 1.6:1
